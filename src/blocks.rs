use std::fs::File;
use std::io::Read;
use serde::Deserialize;
use anyhow::Error;

pub struct Blocks{

}


impl Blocks{

    // pub async fn get_all_blocks(api_url: &str) -> Result<Vec<Blocks>, anyhow::Error> {
    //     // To-Do
    // }
    // pub async fn get_blocks_by_batch(api_url: &str) -> Result<Vec<Blocks>, anyhow::Error> {
    //     // To-Do
    // }
    // pub async fn get_blocks_by_range(api_url: &str) -> Result<Vec<Blocks>, anyhow::Error> {
    //     // To-Do
    //     blocks = 
    //     let response: Result<Vec<Block>, serde_json::Error> = Ok(blocks);
    // }
}