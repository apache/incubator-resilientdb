# Copy this file to a local, untracked configuration and replace every
# placeholder. The final client_num addresses in iplist are client hosts; all
# preceding addresses are replica hosts.

client_num=1
benchmark_seconds=70
ssh_user=ubuntu
remote_home=/home/ubuntu

iplist=(
192.0.2.11
192.0.2.12
192.0.2.13
192.0.2.14
192.0.2.21
)
